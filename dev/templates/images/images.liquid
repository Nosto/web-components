<script type="module" src="../src/main.ts"></script>

<span class="reco-title">{{title }}</span>
{%- for product in imageProducts %}
    <nosto-image src="{{ product.imageUrl }}" width="300" height="300" alt="{{ product.name }}" ></nosto-image>
{%- endfor %}