/**
 * Props for the Control custom element
 *
 * Note: Control component doesn't have any configurable properties,
 * it only uses template children with segment attributes.
 */
export type ControlProps = Record<string, never>
